/* 缓冲IO 实例
* 写入一个结构体变量（以二进制数据形式）到文件中
* 再从文件中读取该数据并打印出来
*/

#include<stdio.h>

struct Book{
    char name[100];
    unsigned int number;
    double price;
};
int main(){
    struct Book B1={"Linux Book!",50,3.14};
    W=fopen("")
    return 0;
}